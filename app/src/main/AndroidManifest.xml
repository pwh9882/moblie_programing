<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile_programing"
        tools:targetApi="31">
        <activity
            android:name=".views.RoutineComplete"
            android:exported="false" />
        <activity
            android:name=".DbDemoActivity"
            android:exported="false" />
        <activity
            android:name=".views.CardDetailActivity"
            android:exported="false" />
        <activity
            android:name=".views.RoutineCreateActivity"
            android:exported="false"
            android:label="@string/title_activity_routine_create"
            android:theme="@style/Theme.Mobile_programing" />
        <activity
            android:name=".views.RoutineProgressActivity"
            android:exported="false" />
        <activity
            android:name=".views.RoutineDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_routine_detail"
            android:theme="@style/Theme.Mobile_programing" />
        <activity
            android:name=".views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.MainActivity" />
    </application>

</manifest>